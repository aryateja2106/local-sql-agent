{"id":"dqe-null-pct","domain":"data_quality","dialect":"sqlite","pipeline_stage":"silver","question":"Find the percentage of silver booking events with a missing customer ID.","sql":"SELECT 100.0 * (COUNT(*) - COUNT(customer_id)) / NULLIF(COUNT(*), 0) AS null_customer_pct FROM silver_booking_events;","explanation":"COUNT(column) excludes NULLs; NULLIF avoids divide-by-zero when profiling a load.","tags":["nulls","profiling"]}
{"id":"dqe-dedup-latest","domain":"data_quality","dialect":"sqlite","pipeline_stage":"bronze_to_silver","question":"Keep the newest bronze version of each event ID using ingestion_id as the tiebreaker.","sql":"WITH ranked AS (SELECT b.*, ROW_NUMBER() OVER (PARTITION BY event_id ORDER BY ingested_at DESC, ingestion_id DESC) AS rn FROM bronze_events AS b WHERE is_malformed = 0 AND event_id IS NOT NULL) SELECT event_id, topic, partition_id, kafka_offset, event_ts, ingested_at, ingestion_id FROM ranked WHERE rn = 1;","explanation":"ROW_NUMBER with a deterministic ORDER BY keeps one winner per business key for Silver dedup.","tags":["window","dedup"]}
{"id":"dqe-source-missing-target","domain":"reconciliation","dialect":"sqlite","pipeline_stage":"redshift","question":"Find source orders that are missing from the target after a warehouse load.","sql":"SELECT s.order_id, s.amount, s.status FROM source_orders AS s LEFT JOIN target_orders AS t ON s.order_id = t.order_id WHERE t.order_id IS NULL;","explanation":"A left anti-join identifies source-only records dropped between stages.","tags":["joins","reconciliation"]}
{"id":"dqe-full-reconcile","domain":"reconciliation","dialect":"sqlite","pipeline_stage":"redshift","question":"Reconcile source and target orders and label source-only, target-only, matched, and amount mismatches.","sql":"SELECT COALESCE(s.order_id, t.order_id) AS order_id, s.amount AS source_amount, t.amount AS target_amount, CASE WHEN s.order_id IS NULL THEN 'target_only' WHEN t.order_id IS NULL THEN 'source_only' WHEN s.amount IS NOT t.amount THEN 'value_mismatch' ELSE 'matched' END AS status FROM source_orders AS s FULL OUTER JOIN target_orders AS t ON s.order_id = t.order_id;","explanation":"FULL OUTER JOIN exposes both missing sides and NULL-safe amount mismatches for DQ reports.","tags":["joins","reconciliation"]}
{"id":"dqe-third-salary","domain":"sql_fundamentals","dialect":"sqlite","pipeline_stage":"interview","question":"Return the third highest distinct salary.","sql":"WITH ranked AS (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS salary_rank FROM employees) SELECT DISTINCT salary FROM ranked WHERE salary_rank = 3;","explanation":"DENSE_RANK handles ties without gaps when asking for the Nth distinct value.","tags":["window","ranking"]}
{"id":"dqe-wow-change","domain":"monitoring","dialect":"sqlite","pipeline_stage":"gold","question":"Calculate day-over-day booking_count change by route from gold metrics.","sql":"WITH daily AS (SELECT metric_date, route, booking_count FROM gold_daily_booking_metrics) SELECT metric_date, route, booking_count, LAG(booking_count) OVER (PARTITION BY route ORDER BY metric_date) AS prior_day_bookings, 100.0 * (booking_count - LAG(booking_count) OVER (PARTITION BY route ORDER BY metric_date)) / NULLIF(LAG(booking_count) OVER (PARTITION BY route ORDER BY metric_date), 0) AS pct_change FROM daily ORDER BY route, metric_date;","explanation":"LAG compares the current aggregate with the previous period for pipeline volume monitoring.","tags":["window","monitoring"]}
{"id":"dqe-bronze-vs-silver","domain":"reconciliation","dialect":"sqlite","pipeline_stage":"bronze_to_silver","question":"Compare bronze accepted rows plus quarantined rows against silver row counts for the latest batch.","sql":"SELECT b.batch_id, b.source_count AS bronze_rows, b.accepted_count AS silver_rows, b.quarantine_count, (b.accepted_count + b.quarantine_count) AS accounted_rows, CASE WHEN b.source_count = (b.accepted_count + b.quarantine_count) THEN 'balanced' ELSE 'imbalance' END AS balance_status FROM batch_control AS b WHERE b.stage = 'bronze_to_silver';","explanation":"Stage balance checks whether every Bronze row was either accepted into Silver or quarantined.","tags":["reconciliation","batch_control"]}
{"id":"dqe-quarantine-reasons","domain":"data_quality","dialect":"sqlite","pipeline_stage":"bronze_to_silver","question":"Show quarantine reason counts for malformed Kafka events.","sql":"SELECT reason, COUNT(*) AS event_count FROM quarantine_events GROUP BY reason ORDER BY event_count DESC;","explanation":"Quarantine reason rollups explain why Bronze rows never reached Silver.","tags":["quarantine","profiling"]}
{"id":"dqe-silver-missing-redshift","domain":"reconciliation","dialect":"sqlite","pipeline_stage":"redshift","question":"Find silver bookings that did not land in the Redshift fact table.","sql":"SELECT s.booking_id, s.customer_id, s.fare_amount, s.event_date FROM silver_booking_events AS s LEFT JOIN redshift_fact_orders AS r ON s.booking_id = r.order_id WHERE r.order_id IS NULL;","explanation":"Left anti-join from Silver/Gold to Redshift finds dropped COPY loads.","tags":["joins","redshift"]}
{"id":"dqe-amount-mismatch-warehouse","domain":"reconciliation","dialect":"sqlite","pipeline_stage":"redshift","question":"Find bookings where silver fare_amount differs from the Redshift fact amount.","sql":"SELECT s.booking_id, s.fare_amount AS silver_amount, r.amount AS redshift_amount FROM silver_booking_events AS s INNER JOIN redshift_fact_orders AS r ON s.booking_id = r.order_id WHERE s.fare_amount IS NOT r.amount;","explanation":"Value mismatches after COPY often mean stale files, partial reloads, or wrong winner after dedup.","tags":["reconciliation","redshift"]}
{"id":"dqe-scd2-current","domain":"scd","dialect":"sqlite","pipeline_stage":"gold","question":"List the current SCD Type 2 customer dimension rows.","sql":"SELECT customer_id, city, loyalty_tier, effective_from, effective_to FROM dim_customer WHERE is_current = 1 ORDER BY customer_id;","explanation":"Type 2 keeps history; is_current = 1 selects the open version for current reporting.","tags":["scd","dimension"]}
{"id":"dqe-scd2-changes","domain":"scd","dialect":"sqlite","pipeline_stage":"gold","question":"Find staged customers that would create a new SCD Type 2 version because the row hash changed.","sql":"SELECT s.customer_id, d.city AS current_city, s.city AS staged_city, d.loyalty_tier AS current_tier, s.loyalty_tier AS staged_tier FROM staged_customer AS s INNER JOIN dim_customer AS d ON s.customer_id = d.customer_id AND d.is_current = 1 WHERE d.row_hash <> s.row_hash;","explanation":"Compare staged row_hash to the current dimension row before closing and inserting Type 2 history.","tags":["scd","hash"]}
{"id":"dqe-scd2-new-keys","domain":"scd","dialect":"sqlite","pipeline_stage":"gold","question":"Find staged customers that do not exist yet in the current dimension.","sql":"SELECT s.customer_id, s.city, s.loyalty_tier FROM staged_customer AS s LEFT JOIN dim_customer AS d ON s.customer_id = d.customer_id AND d.is_current = 1 WHERE d.customer_id IS NULL;","explanation":"New natural keys become insert-only Type 2 rows with is_current = 1.","tags":["scd","joins"]}
{"id":"dqe-null-by-route","domain":"data_quality","dialect":"sqlite","pipeline_stage":"gold","question":"Which routes have the highest null customer percentage in gold metrics?","sql":"SELECT route, metric_date, null_customer_pct, booking_count FROM gold_daily_booking_metrics ORDER BY null_customer_pct DESC, booking_count DESC LIMIT 5;","explanation":"Gold DQ metrics surface routes where customer identity is incomplete after Silver transforms.","tags":["nulls","gold"]}
{"id":"dqe-kafka-coords","domain":"kafka","dialect":"sqlite","pipeline_stage":"bronze","question":"Show Kafka coordinates for the latest bronze ingestion per event_id.","sql":"WITH ranked AS (SELECT event_id, topic, partition_id, kafka_offset, ingestion_id, ROW_NUMBER() OVER (PARTITION BY event_id ORDER BY ingestion_id DESC) AS rn FROM bronze_events WHERE event_id IS NOT NULL) SELECT event_id, topic, partition_id, kafka_offset, ingestion_id FROM ranked WHERE rn = 1 ORDER BY event_id;","explanation":"Topic, partition, and offset are the immutable coordinates used for replay and quarantine lineage.","tags":["kafka","dedup"]}
{"id":"dqe-malformed-rate","domain":"data_quality","dialect":"sqlite","pipeline_stage":"bronze","question":"What percentage of bronze events are malformed?","sql":"SELECT 100.0 * SUM(CASE WHEN is_malformed = 1 THEN 1 ELSE 0 END) / NULLIF(COUNT(*), 0) AS malformed_pct, SUM(CASE WHEN is_malformed = 1 THEN 1 ELSE 0 END) AS malformed_count, COUNT(*) AS total_count FROM bronze_events;","explanation":"Malformed rate is an early Bronze quality signal before Glue cleaning.","tags":["profiling","bronze"]}
{"id":"dqe-top-routes","domain":"analytics","dialect":"sqlite","pipeline_stage":"gold","question":"Show the top 3 routes by total fare in gold metrics.","sql":"SELECT route, SUM(fare_total) AS total_fare, SUM(booking_count) AS bookings FROM gold_daily_booking_metrics GROUP BY route ORDER BY total_fare DESC LIMIT 3;","explanation":"Gold tables answer business questions after Silver validation and aggregation.","tags":["aggregation","gold"]}
{"id":"dqe-duplicate-event-ids","domain":"data_quality","dialect":"sqlite","pipeline_stage":"bronze","question":"Find event IDs that appear more than once in bronze.","sql":"SELECT event_id, COUNT(*) AS versions FROM bronze_events WHERE event_id IS NOT NULL GROUP BY event_id HAVING COUNT(*) > 1 ORDER BY versions DESC;","explanation":"Duplicate event_ids are expected in at-least-once Kafka delivery and must be collapsed in Silver.","tags":["dedup","kafka"]}
{"id":"redshift-null-profile","domain":"data_quality","dialect":"redshift","pipeline_stage":"interview","question":"Write Redshift SQL to profile null customer_id percentage in a booking fact table.","sql":"SELECT COUNT(*) AS total_rows, COUNT(*) - COUNT(customer_id) AS null_customer_ids, 100.0 * (COUNT(*) - COUNT(customer_id)) / NULLIF(COUNT(*), 0) AS null_pct FROM fact_bookings;","explanation":"Same null-profiling pattern interviewers expect for Redshift validation after COPY.","tags":["nulls","redshift"]}
{"id":"redshift-dedup-qualify","domain":"data_quality","dialect":"redshift","pipeline_stage":"interview","question":"Write Redshift SQL using QUALIFY to keep the latest event per event_id.","sql":"SELECT * FROM staging_events WHERE event_id IS NOT NULL QUALIFY ROW_NUMBER() OVER (PARTITION BY event_id ORDER BY updated_at DESC, ingestion_id DESC) = 1;","explanation":"Redshift QUALIFY filters window results without an outer subquery; still requires a deterministic tiebreaker.","tags":["window","redshift"]}
{"id":"redshift-copy-skeleton","domain":"aws_pipeline","dialect":"redshift","pipeline_stage":"interview","question":"Show a Redshift COPY statement that loads curated Parquet from S3 into a booking fact table.","sql":"COPY fact_bookings FROM 's3://airline-datalake/gold/bookings/' IAM_ROLE 'arn:aws:iam::123456789012:role/RedshiftS3Role' FORMAT AS PARQUET;","explanation":"Interview narration: Gold Parquet in the same Region, IAM role, then reconcile counts before marking the batch complete.","tags":["copy","redshift","s3"]}
{"id":"redshift-scd2-close","domain":"scd","dialect":"redshift","pipeline_stage":"interview","question":"Write Redshift SQL that closes current dimension rows when staged attributes change.","sql":"UPDATE dim_customer SET effective_to = staged_customer.effective_from, is_current = FALSE FROM staged_customer WHERE dim_customer.customer_id = staged_customer.customer_id AND dim_customer.is_current = TRUE AND dim_customer.row_hash <> staged_customer.row_hash;","explanation":"Type 2 close step before inserting the new current version; dedupe staged source first for MERGE safety.","tags":["scd","redshift"]}
