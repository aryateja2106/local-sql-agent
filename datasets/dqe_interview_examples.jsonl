{"question":"Find the percentage of orders with a missing customer ID.","sql":"SELECT 100.0 * (COUNT(*) - COUNT(customer_id)) / NULLIF(COUNT(*), 0) AS null_customer_pct FROM orders;","explanation":"COUNT(column) excludes NULLs; NULLIF prevents division by zero."}
{"question":"Keep the newest version of each event ID.","sql":"WITH ranked AS (SELECT e.*, ROW_NUMBER() OVER (PARTITION BY event_id ORDER BY updated_at DESC, ingestion_id DESC) AS rn FROM events AS e) SELECT * FROM ranked WHERE rn = 1;","explanation":"ROW_NUMBER keeps one deterministic winner per business key."}
{"question":"Find source records missing from the target after a load.","sql":"SELECT s.order_id FROM source_orders AS s LEFT JOIN target_orders AS t ON s.order_id = t.order_id WHERE t.order_id IS NULL;","explanation":"A left anti-join identifies source-only records for reconciliation."}
{"question":"Find mismatches between source and target order amounts.","sql":"SELECT COALESCE(s.order_id, t.order_id) AS order_id, s.amount AS source_amount, t.amount AS target_amount FROM source_orders AS s FULL OUTER JOIN target_orders AS t ON s.order_id = t.order_id WHERE s.order_id IS NULL OR t.order_id IS NULL OR s.amount IS NOT t.amount;","explanation":"A full outer join exposes source-only, target-only, and value-mismatch records."}
{"question":"Return the third highest distinct salary.","sql":"WITH ranked AS (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS salary_rank FROM employees) SELECT DISTINCT salary FROM ranked WHERE salary_rank = 3;","explanation":"DENSE_RANK handles ties without gaps."}
{"question":"Calculate week-over-week row-count change.","sql":"WITH weekly AS (SELECT DATE_TRUNC('week', event_date) AS week_start, COUNT(*) AS row_count FROM events GROUP BY 1) SELECT week_start, row_count, LAG(row_count) OVER (ORDER BY week_start) AS prior_week_rows, 100.0 * (row_count - LAG(row_count) OVER (ORDER BY week_start)) / NULLIF(LAG(row_count) OVER (ORDER BY week_start), 0) AS pct_change FROM weekly ORDER BY week_start;","explanation":"LAG compares the current aggregate with its previous period."}
{"question":"Profile null rates for critical booking columns.","sql":"SELECT COUNT(*) AS total_rows, COUNT(*) - COUNT(customer_id) AS null_customers, COUNT(*) - COUNT(fare_amount) AS null_fares FROM silver_booking_events;","explanation":"Critical-column null profiling is a first-pass data-quality check after Silver transforms."}
{"question":"Show quarantine reasons after a Glue cleanse job.","sql":"SELECT reason, COUNT(*) AS rejected_rows FROM quarantine_events GROUP BY reason ORDER BY rejected_rows DESC;","explanation":"Quarantine reason rollups explain why Bronze rows never reached Silver."}
{"question":"Find current SCD Type 2 customer rows.","sql":"SELECT customer_id, city, loyalty_tier, effective_from FROM dim_customer WHERE is_current = 1;","explanation":"is_current selects the open Type 2 version for present-state reporting."}
{"question":"Validate Bronze to Silver batch balance.","sql":"SELECT batch_id, source_count, accepted_count, quarantine_count, CASE WHEN source_count = accepted_count + quarantine_count THEN 'balanced' ELSE 'imbalance' END AS status FROM batch_control;","explanation":"Every Bronze row should be accepted into Silver or sent to quarantine."}
